/**
 * Interface de Cargo que representa a estrutura de um cargo no banco de dados
 * @interface Role
 */
export interface Role {
  id: string;
  name: string;
  description: string;
  created_at: string;
}
