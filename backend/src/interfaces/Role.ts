/**
 * @interface Role
 * @description Interface para Role que representa a estrutura de um cargo no banco de dados
 */
export interface Role {
  name: string;
  description: string;
  created_at: string;
}
